{
  "name": "@ckd/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run --reporter=verbose",
    "check:types": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "firebase": "^12.10.0"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vitest": "^3.2.4"
  }
}
